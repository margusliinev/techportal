import { SearchContainer, JobsContainer } from '../../components';

const Jobs = () => {
    return (
        <>
            <SearchContainer />
            <JobsContainer />
        </>
    );
};

export default Jobs;
